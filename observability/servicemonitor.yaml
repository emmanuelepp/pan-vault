apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: pan-vault
  namespace: monitoring
spec:
  namespaceSelector:
    matchNames:
      - cde
  selector:
    matchLabels:
      app: pan-vault
  endpoints:
    - port: metrics
      interval: 30s
